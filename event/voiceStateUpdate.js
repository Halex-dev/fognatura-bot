module.exports = {
    name: 'voiceStateUpdate',
    description: 'Funzione per quando un utente cambia lo stato della Voce',
    async execute(oldVoice, newVoice){
        /*console.log(oldVoice);
        console.log(newVoice);*/
    }
}

